/** @deprecated Use PartialSuccess instead. */
export declare type PartialSuccessBean = PartialSuccess;
export interface PartialSuccess {
    entries?: {
        issueId?: number;
        issueKey?: string;
        status?: number;
        errors?: string[];
    }[];
}
